<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Berliner</title>
  <style type="text/css">
  <%= style %>
  </style>
</head>
<body style="padding: 10px 20px;">
  <% articles.each do |article| %>
    <article>
      <a href="<%= article.permalink  rescue "Error" %>"><h1><%= article.title rescue "Error" %></h1></a>
      <h2><%= article.author rescue "Error"%></h2>
      <h3><%= article.source rescue "Error"%></h3>
      <%= article.body rescue "Error" %>
    </article>
  <% end %>
</body>
</html>